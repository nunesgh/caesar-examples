// RUN: @caesar @file

@wlp
proc probabilistic_choice(y: Int) -> (x: Int)
    pre [y < 0] * 1/3 + [y == 0] + [y > 0] * 2/3
    post [x >= 0]
{
    var prob_choice: Bool = flip(1/3)
    if prob_choice {
        x = 0 - y
    } else {
        x = y
    }
}
